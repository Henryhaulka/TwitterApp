<section class="container">
<div class="row mt-3">
  <% if user_signed_in? %>
   <%= render 'tweeets/profile' %>
  <% else%>
  <%= render 'tweeets/trend' %>
  <% end %>
  <%= render 'tweeets/feed'%>
  <%= render 'tweeets/who_to' %>
</div>

</section>

